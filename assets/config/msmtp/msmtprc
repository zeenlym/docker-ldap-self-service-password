# Default sSettings for all accounts!
defaults

protocol smtp
domain {{SERVER_HOSTNAME}}

auth off
tls off

auto_from on
syslog on
maildomain {{SERVER_HOSTNAME}}
dsn_notify off
dsn_return off

# Default Account
account default
host {{SMTP_HOST}}
port {{SMTP_PORT}}

