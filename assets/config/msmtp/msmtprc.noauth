# Default sSettings for all accounts!
defaults

protocol smtp

tls {{SMTP_TLS}}
tls_certcheck off

auto_from off
syslog on
dsn_notify off
dsn_return off

# Default Account
account default
auth off
host {{SMTP_HOST}}
port {{SMTP_PORT}}

